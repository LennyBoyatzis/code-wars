var TreeNode = function(value, left, right) {
    this.value = value;
    this.left = left;
    this.right = right;
}

function arrayToTree(array) {
    const test = array.map(something => something);
    return test;
};

console.log("Test", arrayToTree([17, 0 , -4, 3, 15]));
